# EECS678_QuashProject

1. Open the terminal in this directory.
2. Use the makefile command "make run" for default run.
3. Use the makefile command "make valgrind" for default run with valgrind check.